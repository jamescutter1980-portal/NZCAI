node_modules/
.next/
out/
build/
*.tsbuildinfo
next-env.d.ts
.env
.env.local
.env*.local
.DS_Store
npm-debug.log*
/data/
public/maplibre/

# Regenerated by `next dev` / `next build` (generate-agent-files.js), so they
# are build output, not source. Ignored to keep Next version bumps out of the
# diff; delete these two lines if you want the generated rules in the repo.
AGENTS.md
CLAUDE.md
