node_modules/
.next/
out/
coverage/
*.log
.DS_Store
.env
.env.*
!.env.example
# Portal runtime data (SQLite, consents) is ignored, and so is data/reference:
# the DESNZ flat file and CRREM pathways are licensed or annually reissued
# published tables, supplied per deployment and never in version control.
data/
next-env.d.ts
tsconfig.tsbuildinfo

# MCP layer (Python)
.venv/
__pycache__/
*.pyc
.pytest_cache/
dist/
*.egg-info/
