node_modules/
.next/
out/
coverage/
*.log
.DS_Store
.env
.env.*
!.env.example
# Portal runtime data (SQLite, consents) is ignored, and so is data/reference:
# the DESNZ flat file and other published tables are supplied per deployment and
# never enter version control. Only the MCP pathway datasets are committed.
data/*
!data/pathways/
next-env.d.ts
tsconfig.tsbuildinfo

# MCP layer (Python)
.venv/
__pycache__/
*.pyc
.pytest_cache/
dist/
*.egg-info/
