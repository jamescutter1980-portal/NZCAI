import SiteSearch from "@/components/SiteSearch";

export const metadata = {
  title: "Search — NZC AI",
  description: "Find sites by description, location, size, EPC, ownership and grid headroom",
};

export default function SearchPage() {
  return <SiteSearch />;
}
